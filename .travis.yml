language: node_js
# script: 'true' because travis should not run npm test
script: 'true'
notifications:
    email: false
branches:
  only:
  - master
deploy:
  - provider: npm
    email: "rene.heinen@gmx.net"
    api_key: $NPM_TOKEN
    skip_cleanup: true